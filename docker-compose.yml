version: '3.8'

services:
  streamlit:
    build: .
    container_name: plant-disease-detection
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    restart: unless-stopped
